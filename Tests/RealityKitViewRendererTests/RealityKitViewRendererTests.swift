import XCTest
@testable import RealityKitViewRenderer

final class RealityKitViewRendererTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(RealityKitViewRenderer().text, "Hello, World!")
    }
}
